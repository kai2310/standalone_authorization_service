{
  "apiVersion": "0.2",
  "swaggerVersion": "1.2",
  "basePath": "http://<base>/test/v1",
  "resourcePath": "/ui-gateway",
  "apis": [
    {
      "path": "/ui-gateway",
      "description": "Test Endpoints for UI Gateway",
      "operations": [
        {
          "httpMethod": "GET",
          "summary": "A test GET endpoint for the UI Gateway",
          "nickname": "testGet"
        },
        {
          "httpMethod": "POST",
          "summary": "A test POST endpoint for the UI Gateway",
          "nickname": "testPost"
        },
        {
          "httpMethod": "PUT",
          "summary": "A test PUT endpoint for the UI Gateway",
          "nickname": "testPut"
        },
        {
          "httpMethod": "DELETE",
          "summary": "A test DELETE endpoint for the UI Gateway",
          "nickname": "testDelete"
        }
      ]
    }
  ]
}
